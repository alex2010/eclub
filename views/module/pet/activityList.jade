extends inc/main
block content
    #activity.container
        include ../../inc/crumb
        .row
            #main.col-md-8
                #activityList.boxList
                    each it in items
                        include inc/boxItem
            #side.col-md-4
                h3 活动分类
                ul
                    li
                        a(href='/activityList') 全部
                    each it in cats
                        li!= f.link(it)
