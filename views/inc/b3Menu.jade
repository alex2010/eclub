ul(class= mClass || 'nav navbar-nav')
    if c.nav
        each it in c.nav
            li(class= it.code || '')
                a(href='#{it.href}')
                    if it.cls
                        != f.icon(it.cls)
                    = it.label
                if _showSub && it.children
                    ul.dropdown-menu
                        each sub in it.children
                            li
                                a(href='#{sub.href}')
                                    if it.cls
                                        != f.icon(it.cls)
                                    = sub.label