.hostInfo.media
    .media-body
        .media-left.pull-left!= f.img(f.resPath(c, 'portrait/' + _user._id + '.jpg'))
        h4.media-heading!= f.link(_user, 'username')
            small=_user.title
                if _user.industry
                    span &nbsp;-&nbsp;
                    if i18.iCat('industry')
                        span= i18.iCat('industry')[_user.industry]
        != _user.introduction